name: Configuration using Ansible

on: 
#   push:
#     branches:
#       - ansible-16062026


  workflow_dispatch:



jobs:
  test:
    runs-on: ansible-1

    steps:

      - name: Test print 
        run: |
          echo "Hello world"

      - name: install Ansible
        run: python2 -m pip install --user ansible
        

          
    
